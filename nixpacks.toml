[phases.setup]
nixPkgs = ["python311", "ffmpeg"]
aptPkgs = []

[phases.install]
dependsOn = ["setup"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 8080 --timeout-keep-alive 300 --timeout-graceful-shutdown 30 --limit-max-requests 1000"
