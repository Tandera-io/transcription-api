[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 300 --timeout-graceful-shutdown 30 --limit-max-requests 1000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
